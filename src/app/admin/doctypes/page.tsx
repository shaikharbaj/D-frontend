import React from "react";
import { List } from "@/views/admin/doctype";

interface IPageProps {}

const Page: React.FunctionComponent<IPageProps> = async (props) => {
  return <List />;
};

export default Page;
